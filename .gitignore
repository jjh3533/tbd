# 시크릿
.env
.env.*
!.env.example

# 기기별 로컬 설정 오버라이드 (naver_config.py 맨 아래에서 있으면 import) - 두 맥
# 간에 공유하면 안 되는 값이 생기면 여기 채워 넣고 커밋하지 않기 위함
naver_config_local.py

# Streamlit
.streamlit/secrets.toml

# NiceGUI 로컬 스토리지 캐시 (사용자별 세션/테마 저장 파일)
.nicegui/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# 대용량 이미지 자산 (Product Images는 로컬/Drive에만 보관, git으로 관리하지 않음)
Product Images/

# 에이전트/도구 메타데이터
.agents/
.claude/

# 실행 로그 (루트에 다시 쌓이지 않도록 - 필요한 로그는 archive/logs/에 수동 보관)
*.log
