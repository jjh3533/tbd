# 작업 이력

각 세션에서 완료한 작업의 시간순 기록. 지금 상태만 빠르게 보려면 `CLAUDE.md`를
참고하세요 — 이 파일은 "언제 왜 그렇게 했는지"를 나중에 다시 찾아볼 때만 필요합니다.

1. UniFi 스위치 상세페이지 3개 신규 생성 (Pro Max 16/16 PoE/Flex 10 GbE) — `build_pages.py` 패턴 확립, 랙마운트 제품 이미지의 반사 중복 아티팩트 크롭, Playwright 기반 PNG export 파이프라인 구축 (Claude Design 없이 재현)
2. 한글 줄바꿈(`word-break: keep-all`)과 "급전" 등 어색한 표현 전체 수정
3. 네이버 배송/원산지 설정 수정: 택배사 CJGLS→ACE, 원산지 중국→미국, 반품/교환비 3천/6천→4만/8만원. `visitAddressId`는 **필드 자체를 빼야** "직접수령 불가능"으로 처리됨(0을 넣으면 거부)을 확인. `OUTOFSTOCK` statusType을 GET에서는 주지만 PUT에서는 거부하는 버그도 발견/수정
4. **대시보드 전면 교체**: Streamlit(CSS 커스터마이징이 힘들다는 사용자 불만) → NiceGUI. `sync_engine.py`로 백엔드 로직 분리, `dashboard/` 패키지 신축
5. 디자인 피드백 반영 2라운드: 페이지 배경≠카드 배경으로 깊이감 부여, 통계카드 높이 불균일 수정, NiceGUI 버튼 기본 파란색 이슈(Tailwind `!bg-[...]` 강제 클래스로 해결), 활성 메뉴를 검정→흰색 배경으로 변경
6. NAS(Synology, `192.168.50.245`)에 실제 배포 → `https://my.tbd.kr` (Cloudflare Tunnel). 전용 SSH 키 설정, Synology SFTP 서브시스템 미작동 이슈(`scp -O` 사용), 홈디렉토리 권한 이슈 해결
7. **보안 수정**: `naver_config.py`에 하드코딩되어 **public 레포에 평문 커밋**되어 있던 네이버 Client Secret 발견 → `.env`로 이전, 사용자가 재발급, 새 값 동작 확인 완료. **주의: git 히스토리 자체는 아직 정리 안 함 (재발급으로만 방어됨)**
8. Switching 카테고리 6개 상세페이지 추가 생성 (Enterprise 8 PoE, Flex, Flex Utility, Flex Utility Pro, Pro 8 PoE, Pro XG 8 PoE). **중요**: 리서치 결과 "Flex Utility"/"Flex Utility Pro"는 스위치가 아니라 빈 방수 인클로저(액세서리)로 확인 → 정직하게 인클로저 스펙 + "스위치 별도구매" 명시로 작성
9. 통관/관부가세 안내 문구 오류 수정 (29개 파일 전체 일괄 반영 + 템플릿 소스도 수정)
10. 신규 6개 상품 실제 네이버 등록 완료. 과정에서 `PRODUCT_PAGES_DIR`이 존재하지 않는 옛 경로를 가리키던 버그 발견/수정, 재고 0 신규등록 거부 이슈 확인/처리
11. NocoDB 동기화 중 검색어 "Flex"가 엉뚱한 상품에 매칭되는 버그 발견/수정
12. 기존 20개 라이브 상품의 통관 이미지를 새 문구로 교체 (라이브/로컬 이미지 개수 검증 후 안전하게 위치 매칭)
13. NocoDB Product 테이블의 `Product_Page` 필드를 상세페이지 보유 제품 전체(29개)에 "Detail"로 설정 (기존 20개 → 29개)
14. 신규 3개 상품 네이버 스마트스토어 등록 완료 (Pro Max 16 / Pro Max 16 PoE / Flex XG = Flex 10 GbE). Flex XG는 NocoDB 재고 0이라 5개로 등록 후 `update_price_stock.py`로 즉시 품절 처리
15. `sync_naver_ids_to_nocodb.py` `TARGET_PRODUCTS`에 신규 3개(Pro Max 16, Pro Max 16 PoE, Flex XG) + 기존에 누락되어 있던 UCG Industrial 추가 — 전체 29개 채널상품번호 NocoDB 반영 완료
16. `update_price_stock.py` 전체 실행 — 29개 전 상품 가격/재고 네이버 동기화 완료
17. **WiFi 카테고리 16개 제품 상세페이지 생성 및 네이버 등록 완료** (AC Pro, Building Bridge XG, Device Bridge, Device Bridge Switch, E7 Campus, U6 Enterprise, U6 Enterprise In-Wall, U6 In-Wall, U6 Mesh, U6 Mesh Pro, U6+, U7 Outdoor, U7 Pro Outdoor, U7 Pro Wall, U7 Pro XG Wall, U7 Pro XGS). WiFi 5/6/6E/7 전 세대 + 무선 브리지 + 옥외 메시 등 다양한 라인업 커버. 4개 배치로 나눠 순차 생성(`gen_wifi_batch1~4.py`), 이미지 자동 처리(`process_wifi_images.py`), PNG export(`export_wifi_pages.py`), 네이버 등록(채널상품번호 13686839735~13686840578), NocoDB 동기화 완료
18. `sync_naver_ids_to_nocodb.py` `TARGET_PRODUCTS`에 16개 WiFi 제품 매핑 추가 — 전체 46개 채널상품번호 NocoDB 반영 완료
19. `update_price_stock.py` 전체 실행 — 46개 전 상품 가격/재고 네이버 동기화 완료
20. **Physical Security 카테고리 13개 제품 상세페이지 생성 및 네이버 등록 완료** (G6 Pro 360, AI PTZ Industrial, G5 Turret Ultra, G6 Dome, AI Theta, All-In-One Sensor, Glass Break Sensor, Motion Sensor, NVR Instant, CloudKey+, AI Horn Speaker, SuperLink Gateway, Floodlight). 카메라(6개, CCTV 카테고리), 센서(3개, AP 카테고리), 녹화/컨트롤(2개, AP 카테고리), 기타(2개, AP 카테고리)로 구성. 4개 배치로 나눠 순차 생성(`gen_ps_batch1~4.py`), 이미지 자동 처리(`process_ps_images.py`), PNG export(`export_ps_pages.py`), 네이버 등록(채널상품번호 13686870764~13686872710), NocoDB 동기화 완료. **카테고리 이슈**: 일부 제품이 도서 카테고리로 오인식되는 문제 발견 → AP 카테고리(50001623)로 통일하여 해결
21. `sync_naver_ids_to_nocodb.py` `TARGET_PRODUCTS`에 13개 Physical Security 제품 매핑 추가 — 전체 59개 채널상품번호 NocoDB 반영 완료
22. `update_price_stock.py` 전체 실행 — 59개 전 상품 가격/재고 네이버 동기화 완료
23. `product_builder.py` 수정: 할인가=판매가일 때 할인 금액 0으로 `customerBenefit` 생성하던 버그 수정 (네이버 API가 거부), 무료배송일 때 `deliveryFeePayType` 필드를 빼도록 수정 (유료배송일 때만 필수)
24. **Physical Security 13개 제품 히어로 이미지 크롭 문제 발견 및 수정**: 검증 결과 13개 제품 모두 원본(1500x1500)의 21-49%만 남기고 과도하게 잘림. 원인은 `crop_hero.py`가 반사가 없는 이미지에서 잘못된 분할점을 찾은 것. `fix_ps_hero_images.py`로 원본을 크롭 없이 복사 → PNG 재export → 네이버 13개 제품 전체 히어로 이미지 업데이트 완료 (`update_ps_hero_images.py`)
25. **WiFi 카테고리 히어로 이미지 검증**: 네이버 등록된 34개 WiFi 제품 중 U6 Mesh Pro 1개만 높이 1494px로 다른 제품(평균 1829px)보다 작음 확인. 원본 1500x1500의 45%만 사용. assets 폴더에 원본 복사 → HTML/PNG 재생성 → 네이버 업데이트 완료
26. **`crop_hero.py` 안전장치 추가**: 반사 감지 실패 케이스(score > 0.3 또는 split 높이 비율 < 50%)에서 원본 그대로 저장하도록 수정. 이제 반사가 없는 이미지가 잘못 크롭되지 않음
27. **Door Access 카테고리 31개 제품 상세페이지 생성 및 네이버 등록 완료** (Reader Pro, Reader Flex, Access Ultra, Door Hub, Door Hub Mini, Enterprise Access Hub, Intercom Viewer, G6 Entry, Magnetic Lock, Access Button, Reader Junction Box, Reader Pro Junction Box, Reader Pro Angle Mount, Intercom Viewer Table Stand, Intercom Flush Mount, Intercom Surface Angle Mount, Intercom Wedge Mount, Intercom Sunshield, Gate Hub, Junction Utility, Door Lock Relay Cable, Door Closer, PoE Over 2-Wire Retrofit Extender, Retrofit Hub, Retrofit PSU 12V, Panic Bar, Access Rescue KeySwitch, Access Card 10-Pack, Pocket Keyfob 10-Pack, Gate Starter Kit, G3 Elevator Starter Kit). 출입통제 리더기/도어락/인터콤/액세서리 전체 라인업 커버. 10개는 Detail 버전(다이어그램 포함), 21개는 Simple 버전(히어로+Design+Tech Specs+공용 섹션만)으로 구성. 3개 배치로 나눠 순차 생성(`gen_door_access_batch1~3_simple.py`), 이미지 자동 처리(`process_door_access_images.py`), 히어로/다이어그램 이미지 표준화(`standardize_door_access_hero.py`, `standardize_door_access_diagram.py`), PNG export(`export_door_access_pages.py`), 네이버 등록(채널상품번호 13686915258~13686917736), NocoDB 동기화 완료
28. `sync_naver_ids_to_nocodb.py` `TARGET_PRODUCTS`에 31개 Door Access 제품 매핑 추가 — 전체 90개 채널상품번호 NocoDB 반영 완료
29. `update_price_stock.py` 전체 실행 — 90개 전 상품 가격/재고 네이버 동기화 완료
30. **Integrations 카테고리 8개 제품 상세페이지 생성 완료** (Mobile Router Industrial, UNAS 2, Display Cast Lite, Mobile Router, 5G Max, Mobile Router Ultra, PoE Audio Port, LTE Backup Pro). 모바일 라우터/5G 장비/NAS/오디오 인터페이스 등 다양한 통합 솔루션. 2개 배치로 나눠 순차 생성(`gen_integrations_batch1~2.py`), 이미지 자동 처리(`process_integrations_images.py`), 히어로 이미지 반사 제거(`crop_integrations_heroes.py`), PNG export(`export_integrations_pages.py`) 완료
31. **Integrations 6개 제품 네이버 등록 완료** (5G Max, LTE Backup Pro, Mobile Router, Mobile Router Ultra, PoE Audio Port, UNAS 2). 가격이 0원인 2개 제품(Display Cast Lite, Mobile Router Industrial)은 NocoDB 가격 계산 확인 필요로 보류. 채널상품번호 13686935032~13686935352
32. `sync_naver_ids_to_nocodb.py` `TARGET_PRODUCTS`에 6개 Integrations 제품 매핑 추가 — 전체 96개 채널상품번호 NocoDB 반영 완료
33. `update_price_stock.py` 전체 실행 — 96개 전 상품 가격/재고 네이버 동기화 완료
34. **NocoDB 필드명 변경 및 가격 정책 정리**: 사용자 요청으로 네이버 판매가는 무조건 할인 없이 NocoDB `판매금액` 필드 값으로 판매. `update_price_stock.py`가 이미 `판매금액`을 사용하므로 Integrations 6개 제품 모두 정상 가격(888,000원 등)으로 등록되어 있음을 확인. `최종가격` 필드명을 `구매원가`로 변경 — 코드 6개 파일 수정(`sync_engine.py`, `app.py`, `export_prices_for_naver.py`, `add_ps_to_excel.py`, `nocodb_fix_fields.py`, `fix_integrations_prices.py`), `sync_engine.py`는 하위 호환성 지원(`구매원가` 또는 `최종가격` 둘 다 읽음). NocoDB UI에서 사용자가 직접 필드명 변경 완료
35. **카테고리 자동 최적화 시스템 구축**: 상품명 기반으로 적합한 네이버 카테고리를 자동 선택하는 `product_keywords.py` 모듈 생성. NAS→저장장치>NAS(50001602), 모바일 라우터→네트워크장비>라우터(50001622) 등 제품 특성에 맞는 카테고리 매핑. `product_builder.py`에 통합되어 신규 상품 등록 시 자동 적용
36. **검색 키워드 자동 생성 시스템 구축**: 카테고리별 맞춤 검색 키워드를 상품명에 자동 추가하는 기능 개발. WiFi(와이파이 끊김 해결, 메시 네트워크), Switching(PoE 급전, 네트워크 확장), Physical Security(실시간 모니터링, 야간촬영), Door Access(출입통제, 무인 출입) 등 유즈케이스 키워드 + 기술 키워드(WiFi7, PoE, AI, 메시 등) 자동 조합. 상품명 형식: `영문명 / 한글명 키워드1 키워드2...` (최대 10개). `product_builder.py`에 통합되어 신규 상품 자동 적용
37. **기등록 상품 카테고리 일괄 변경**: `update_categories.py`로 6개 상품 카테고리 변경 완료 — UNAS 2(NAS 카테고리), Mobile Router/5G Max/Mobile Router Ultra/PoE Audio Port/LTE Backup Pro(라우터 카테고리). Door Access 31개는 디지털도어록 카테고리로 변경 시도했으나 KC 인증 정보 필수로 실패, 현재 네트워크장비>AP 유지 (실제로 UniFi 스마트 보안 장비에 적합)
38. **기등록 상품 검색 키워드 일괄 추가**: `update_product_names_with_keywords.py`로 96개 상품 중 58개 성공적으로 업데이트. 38개는 네트워크 연결 문제로 중단 (재실행 필요). 상품명에 카테고리별 맞춤 키워드 추가되어 검색 유입 최적화 완료
39. **Streamlit 대시보드 제거**: NiceGUI로 완전 전환 완료. `app.py` 삭제, `config.py`에서 Streamlit secrets 로직 제거, `.claude/launch.json`에서 Streamlit 설정 제거. 대시보드는 NiceGUI만 사용 (`https://my.tbd.kr`)
40. **NocoDB 필드명 한글→영문 마이그레이션**: 한글 필드명 인코딩 문제로 대시보드에서 데이터가 0으로 표시되는 이슈 해결. 판매금액→`sale_price`, 구매원가→`purchase_cost`, 수익→`profit`로 변경. 5개 핵심 파일 수정 (`sync_engine.py`, `update_price_stock.py`, `export_prices_for_naver.py`, `add_ps_to_excel.py`, `nocodb_fix_fields.py`), NocoDB UI에서 수동으로 필드명 변경, NAS 배포 완료. `FIELD_MIGRATION.md` 마이그레이션 문서 작성
41. **NAS SSH 설정 업데이트**: `sshpass` 설치, SSH 주소 `jay@192.168.50.245`, 비밀번호 인증 사용, docker-compose 전체 경로 `/usr/local/bin/docker-compose` 사용
42. **가격/재고 변동 이력 저장 + 15일 이상 품절 상품 관리 구축**: 매 동기화 라운드에서 스크래핑한 값이 이전 값과 실제로 다를 때만 영구 기록으로 남기는 시스템 신규 구축(추가 크롤링 없음, 이미 조회한 데이터를 버리지 않고 쌓는 것이 핵심). NocoDB에 `Price_History` 테이블 신규 생성(base `pqlruah70l8clo1`, EAV 스타일: SKU/Naver_Product_No/Field_Name/Old_Value/New_Value/Changed_At) — `create_price_history_table.py`로 생성, table_id는 `.env`/`.env.example`/`config.py`의 `NOCODB_HISTORY_TABLE_ID`(선택 값, 없어도 기존 스크립트 안 깨짐). `sync_engine.py`에 `history_table` 인스턴스 + `_log_change()` 헬퍼(실패해도 조용히 무시, 메인 동기화 안 막음) 추가하고, `process_single_record()`에서 `In_Stock` 변화 및 이번 라운드에 실제로 재조회한 리테일러 가격 변화만 로깅하도록 연결. `Products` 테이블 스키마는 건드리지 않고, "품절 시작일"은 `Price_History`에서 역산(`get_long_oos_products`) — 이 기능 도입 이전부터 품절이었던 상품은 이력이 없어 "기록 이전부터 품절"로 별도 분리. `get_price_history()`로 최근 변동 피드 제공. 새 대시보드 페이지 `dashboard/pages/inventory.py`(`/inventory`)에서 두 섹션(15일 이상 품절 / 최근 변동)을 기존 `uic-table`/`uic-pill` 스타일 그대로 재사용해 렌더링, `dashboard/layout.py` 사이드바에 "📦 재고/이력 관리" 메뉴 추가. 실제 크롤링 없이 테스트 레코드로 create/조회/KST 변환/OOS 계산 검증 완료, NiceGUI 대시보드에서 `/inventory` 렌더링 확인 완료(현재 96개 등록 상품 중 미판매 85개 전부 "기록 이전부터 품절"로 정상 분류 — 이력이 이제 막 쌓이기 시작했으므로 예상된 결과).
43. **가격/재고 이력 기능 NAS 배포 완료**: NAS `.env`에 `NOCODB_HISTORY_TABLE_ID=mi258r3q4g5wu69` 추가, `sync_engine.py`/`config.py`/`nocodb_client.py`/`dashboard/` 전체를 `/volume1/docker/nicegui/`에 업로드 후 `docker-compose restart`. 재시작 로그에 새 에러 없음(기존에 있던 무관한 봇 스캐닝/연결 끊김 로그만 존재), `https://my.tbd.kr/inventory` 실제 200 OK + 정상 콘텐츠 확인 완료. **검증 중 실수**: 로컬 검증 단계에서 다크모드 토글인 줄 알고 누른 버튼이 실제로는 "B&H 개별 Sync" 버튼이라 실제 Scrape.do 크롤링이 몇 초간 발생 — 바로 서버를 내려서 멈췄고 크레딧 잔여량(221,563/250,000)엔 문제 없음.
44. **가격/재고 자동 동기화 스케줄러 구축 및 배포 완료**: 매일 09:00 KST 전체 리테일러(Adorama/Amazon/B&H) 풀 싱크 + 4시간마다 "확인 필요" 상품만 재조회하는 자동화. Synology Task Scheduler/cron 대신 컨테이너 안에서 자체적으로 도는 방식(APScheduler)으로 구현 — `docker-compose`가 이미 `restart: always`라 컨테이너가 항상 떠 있고, 코드가 그대로 버전관리/배포되는 게 장점. `sync_engine.py`에 `start_background_scheduler()`(daily_full_sync: `CronTrigger(hour=9, minute=0, timezone=Asia/Seoul)` / needs_check_sync: `IntervalTrigger(hours=4)`) + `_run_scheduled_sync()`(겹쳐 도는 것 방지용 non-blocking `threading.Lock`, 잠겨있으면 이번 회차는 건너뛰고 로그만 남김) + `_HeadlessLogAdapter`(UI 없이 돌 때 `.write()`를 `print`로 흘려보내 `docker-compose logs`에서 보이게 함) 추가. 기존 `run_tbd_tracker()`를 프레임워크 독립적으로 그대로 재사용(수동 Sync 버튼과 동일 함수, 끝나면 기존처럼 Telegram 요약도 그대로 발송됨). `dashboard/app.py`에서 `ui.run()` 호출 전에 1회 시작(`reload=False`라 프로세스당 정확히 한 번만 실행되므로 중복 스케줄러 걱정 없음). **의존성 추가**: `apscheduler`, `pytz`를 `dashboard/deploy/Dockerfile`의 pip install 목록에 추가 — 이 때문에 이번 배포는 평소 `docker-compose restart`가 아니라 **`docker-compose up -d --build`로 이미지 재빌드가 필요했음** (Dockerfile이 바뀌면 항상 재빌드 필요, 코드만 바뀐 경우는 기존처럼 restart로 충분). 로컬에서 `run_tbd_tracker`를 몽키패치해 실제 크롤링 없이 스케줄 등록/중복 시작 방지/락 동작을 검증한 뒤 NAS에 배포, 재빌드 로그에 에러 없음 확인 + `https://my.tbd.kr/` 및 `/inventory` 200 OK 확인 완료.
45. **레포 정리 + CLAUDE.md 재정리**: 루트 `.py` 파일 52개 + `product_pages/scripts/` 32개 중 이미 끝난 1회성 조사/디버그/검증/수정 스크립트(다른 코드에서 import되지 않고, CLAUDE.md 핵심 파일 표에도 없던 것들)를 `archive/{scripts,product_pages_scripts,data,logs}/`로 이동(전부 `git mv`/`mv`+`git add`, 삭제 없음). 루트 `.py`는 52개→22개, `product_pages/scripts/`는 32개→3개(`build_pages.py`/`crop_hero.py`/`export_sections.py`만 유지)로 축소. `registered_log.json`/`product_slug_map.json`은 각각 `main.py`/`sync_engine.py`가 실제로 읽는 핵심 데이터라 루트에 유지 확인. 로그 재발생 방지로 `.gitignore`에 `*.log` 추가. CLAUDE.md의 44개 항목짜리 "완료된 작업" 섹션을 이 `HISTORY.md`로 분리하고, CLAUDE.md는 "지금 상태" 위주로 축소(235줄 → 약 120줄).
46. **맥 두 대 환경 호환**: `/Users/cheil/tbd`와 `/Users/jay/tbd` 두 macOS 계정에서 같은 저장소를 쓰는데, `naver_config.py`의 `PRODUCT_IMAGES_DIR`/`PRODUCT_PAGES_DIR`과 `update_live_customs_image.py`의 `GD`가 `/Users/cheil`로 하드코딩돼 있어서 다른 계정에서는 구글 드라이브 폴더를 못 찾는 문제 발견. 두 기기 모두 같은 구글 드라이브 계정(`jjh3533@gmail.com`)을 쓰므로 `os.path.expanduser("~")`로 홈 디렉토리만 자동 치환하도록 수정 - 기기별 설정 없이 그대로 동작. `naver_config.py` 하단에 이미 준비돼 있던 `naver_config_local.py` 오버라이드 훅이 `.gitignore`에 빠져 있던 것도 함께 추가(안 그러면 로컬 전용 설정이 그대로 커밋되어 기기 간 섞일 위험).
47. **화이트/블랙 색상 옵션 관리 체계 도입**: 네이버에 이미 "색상"(화이트/블랙) 옵션으로 등록된 상품들(예: U7 Pro XG - 실제 라이브 조회로 optionCombinations에 화이트/블랙 옵션 확인)이 구매처별로 색상 간 가격이 다른 경우가 있는데도, 지금까지 `update_price_stock.py`가 모든 옵션에 같은 가격/재고를 균일 적용하고 있던 문제를 발견/수정. 사용자 요청에 따라 화이트 로우를 기준으로 35개 제품에 대해 `create_color_variant_rows.py`로 `{화이트 SKU} Black` NocoDB 로우 신규 생성(Category/Weight_KG/MSRP_USD/Naver_Product_No는 화이트와 동일 복사, ADORAMA_ID/ASIN/BH_ID는 비워둠 - 사용자가 직접 조사해서 채워 넣을 예정). 목록 중 `UniFi Reader`/`UniFi G3 Reader Fingerprint`/`UniFi Retrofit Reader Fingerprint`는 화이트 자체가 NocoDB에 없어 이번엔 제외. `update_price_stock.py`에 `option_overrides` 파라미터 추가 - Black 로우에 `sale_price`가 채워지면(구매처 ID 스크래핑 완료) 네이버 "화이트"/"블랙" 옵션 조합에 각각 다른 price(추가금액)/stockQuantity를 반영하고, 아직 없으면 기존처럼 균일 적용 + 콘솔 안내. `find()`가 새 Black 로우 때문에 기존 96개 상품 매칭에 혼선을 주지 않는지, Exchange_Rate를 임시로 채워 실제 옵션 분기가 정상 작동하는지(이후 원복) 검증 완료.
48. **B&H 코드 검증 + Product_Page="Clone" 컨벤션 반영**: 사용자가 35개 Black 로우 중 27개에 BH_ID를 입력한 뒤, 실제 B&H 페이지(ld+json 구조화 데이터의 `name`)를 전부 조회해 NocoDB 상품명과 대조 - 26건 정확히 일치, `UniFi Doorbells Black`(처음엔 "Doorbell Lite"라 다른 제품처럼 보였으나 화이트 로우의 기존 BH_ID도 동일하게 "Doorbell Lite"로 확인되어 오탐으로 판명)까지 포함해 실제 불일치는 없음. `UniFi Access Button Black`(1855250-REG)만 제목에 "(Black)" 표기가 없어 사용자 직접 확인 권장으로 남김. 사용자가 NocoDB `Product_Page` 필드에 `None`/`Simple`/`Detail`에 이어 `Clone`(색상 옵션 클론 로우 - 독립된 상세페이지/등록 불필요) 옵션을 도입해 35개 Black 로우 전체에 수동 태깅함을 확인(정확히 1:1 매칭). `create_color_variant_rows.py`가 앞으로 생성하는 로우에는 이 값을 자동으로 채우도록 수정.
49. **화이트/블랙 색상 옵션 네이버 반영 + 옵션 신규 생성 지원**: 사용자가 Black 로우 27개에 BH_ID를 입력하고 Sync를 돌린 뒤 `update_price_stock.py`를 실행했는데, 색상 옵션이 필요한 15개 중 **1개(U7 Pro XG)만 실제로 반영되고 나머지 14개는 조용히 무시됨**을 발견 - 기존 `apply_price_stock()`은 "이미 있는 옵션 조합을 수정"만 할 뿐 "없던 옵션을 새로 만드는" 로직이 없어서, 라이브에 옵션 구조가 아예 없던 상품(단일가로만 등록됐던 상품들)은 계산한 블랙 가격이 갈 곳이 없었던 것. `build_new_color_option_info()`를 추가해 옵션이 없는 상품엔 네이버 PUT으로 "색상"(화이트/블랙) 옵션 조합을 새로 생성하도록 `apply_price_stock()`을 확장(U7 Pro XG의 기존 라이브 optionInfo 구조를 그대로 참고해 구성). 이 과정에서 네이버가 옵션 신규 생성 시 "옵션가 0원 + 재고 1개 이상인 옵션이 최소 1개 있어야 한다"는 `NoZeroStock` 검증을 요구한다는 걸 발견(화이트 재고가 0인 2개 상품에서 400 에러로 확인) → 화이트 재고가 0이면 재고 있는 블랙을 0원 기준으로 자동 전환하고 화이트 쪽에 추가금액을 얹는 앵커 전환 로직 추가(최종 판매가는 동일). 화이트/블랙 둘 다 품절인 경우(`UniFi Reader Pro`)는 옵션 생성 자체가 불가능하므로 이번 라운드는 단일가로 건너뛰고 다음 Sync에 자동 재시도되게 함. 전체 96개 재실행 결과 에러 0건, 15개 중 14개 정상 반영 확인(1개는 품절로 보류).
50. **프로젝트 폴더 이동**: `~/tbd` → `/Users/cheil/Desktop/dev/tbd`. `mv`로 통째 이동(git 히스토리/`.env`/`.git` 전부 그대로 유지), 이동 직후 `git status`/`git log`/핵심 파일 `py_compile`로 무결성 확인. 저장소 안에 이 경로를 하드코딩한 활성 파일은 없음(archive/의 옛 1회성 스크립트에만 남아있고, 이미 실행 안 함). CLAUDE.md의 디렉토리 표기 갱신. **주의**: 다른 한 대(`jay` 계정)는 아직 `/Users/jay/tbd`에 있을 수 있어 두 기기 경로가 지금은 다름 - 맞추려면 그쪽에서도 동일하게 이동 필요.
51. **대시보드 카운팅 버그 수정 + 가격/재고 갱신**: 대시보드 "판매 가능"이 82인데 실제 네이버 판매중은 73이라는 사용자 제보 → `Product_Page="Clone"`인 색상 옵션 로우(9개가 재고 있음) 82-9=73을 확인, `exclude_clone_rows()`를 `home.py`/`category.py`에 적용해 실제 등록 상품 기준으로만 집계하도록 수정. 전체 195개 로우 재크롤링 + `update_price_stock.py`로 네이버 96개 전체 재반영(에러 0건, `UniFi Reader Pro`는 이번에도 화이트/블랙 둘 다 품절이라 옵션 생성 보류 유지).
52. **대시보드 UI 개선 5종**: (1) 상품이 없는 `Advanced Hosting`/`Accessories`를 `CATEGORIES`에서 제거(6개만 유지). (2) 신규 `/needs-check` 페이지(`dashboard/pages/needs_check.py`) - Needs_Check=True 상품만 모아보기, 사이드바에 "⚠️ 확인 필요" 추가. (3) `build_products_table_html`에서 $0(가격 정보 없음) 셀을 회색으로 표시하도록 수정 - Best Price도 데이터 없음(회색)과 "MSRP보다 비쌈"(빨강)을 분리. (4) `get_latest_price_deltas()` 신규 추가 - Price_History에서 SKU+리테일러 필드별 가장 최근 변동을 조회해, B&H/Adorama/Amazon 가격이 지난 기록과 다르면 가격 옆에 ▲(빨강, 상승)/▼(초록, 하락) 표시. (5) UniFi Store($) 컬럼이 이미 `store.ui.com` 링크를 갖고 있었음을 확인(156/160), `_normalize_store_name()`으로 슬래시/팩 표기 차이를 완화 매칭해 나머지 4개(Enterprise Audio/Video Bridge 등)도 연결해 160/160 완성. 로컬 대시보드에서 전부 실제 렌더링/computed style로 검증 완료(카테고리 메뉴, 확인 필요 페이지, $0 회색, 화살표 색상/툴팁, 스토어 링크).
53. **UniFi Store 링크 NAS 배포 누락 발견/수정**: 사용자가 "UniFi Store 금액을 클릭해도 store.ui.com으로 안 간다"고 제보 - 실제 `my.tbd.kr`에서 확인해보니 링크가 전혀 없었음(로컬은 정상). 원인은 `product_slug_map.json`이 NAS에 한 번도 업로드된 적이 없었던 것 - `_load_unifi_store_slug_map()`이 파일 없으면 에러 없이 빈 매핑을 반환하도록 짜여 있어서(디자인상 의도된 방어 코드) 조용히 실패하고 있었음. `.py` 코드만 배포하고 이 데이터 파일을 빠뜨린 게 원인. `product_slug_map.json`을 NAS에 업로드 + 컨테이너 재시작, `my.tbd.kr`에서 실제 링크 160/160 확인 완료. CLAUDE.md NAS 배포 절차에 "데이터 파일도 빠뜨리지 말 것" 경고 추가.
54. **아마존 ASIN 후보 검색 도구 구축 + 55개 반영**: 사용자가 "상품명/모델명으로 아마존에 검색해 후보를 찾아줄 수 있냐"고 물어봐서, 기존 Amazon PDP 스크래핑(`fetch_amazon_info`)과 같은 계정으로 쓸 수 있는 Scrape.do의 Amazon Search 전용 플러그인(`/plugin/amazon/search`, ASIN/제목/가격을 구조화된 JSON으로 반환)을 찾아 실제 호출로 검증. 모델명이 검색 결과 제목에 "독립된 토큰"으로 정확히 들어있는 것만 후보로 인정하는 매칭 로직을 구현하는 과정에서, 짧은 모델명이 더 긴 다른 모델명의 접두어인 경우(예: "U5G"가 "U5G-Max-Outdoor"에 잘못 매칭) 오탐이 발생하는 걸 발견 - 하이픈은 모델명 내부 구분자로 유지하고 그 외 구두점만 공백으로 바꾼 뒤 단어 경계 정규식으로 걸러내도록 수정해 해결(화이트/블랙 모델명 접두어 관계에도 똑같이 적용되어 정확히 구분됨). `search_amazon_candidates.py` 스크립트로 ASIN 없는 166개(화이트 131 + 블랙 35) 전체를 검색해 59개 후보를 CSV로 제안(NocoDB에는 안 씀), 사용자가 직접 검토해 9개를 오탐으로 표시하고 나머지 55개를 확정 - 중복 후보가 있던 9개는 저렴한 가격 쪽으로 골라 NocoDB ASIN 필드에 반영 완료(테스트 1개 → 나머지 54개, 에러 0건). ASIN 보유 상품 84개로 증가. 검토 결과 CSV는 `archive/data/asin_candidates.csv`로 보관.
55. **Sync 버튼 겹침 사고 → 겹침 방지/스피너/중지 버튼 도입**: 사용자가 대시보드에서 "전체", "Amazon", "확인필요" Sync 버튼을 짧은 시간에 연달아 눌러 세 개가 동시에 겹쳐 도는 사고 발생 - Scrape.do 크레딧이 8초에 40~60개씩 빠르게 소모되고 동시 요청이 계정 한도(10개)에 가깝게 찬 것을 실시간 크레딧/동시요청 조회로 확인해 원인 진단. 기존에 스케줄러 전용으로만 쓰던 `_sync_lock`을 수동 버튼도 함께 쓰도록 확장한 `run_sync_guarded()`를 신규 도입(진행 중이면 새 요청 거절), NocoDB에 이미 반영된 결과는 그대로 두고 컨테이너 재시작으로 진행 중이던 요청만 중단(임시 조치). 재발 방지 위해 사용자가 "진행중 아이콘/중지 버튼/Sync 중엔 버튼 전부 비활성화"를 요청 - `run_tbd_tracker()`에 `cancel_event` 파라미터를 추가해 결과 처리 루프마다 확인하고, 취소되면 `ThreadPoolExecutor.shutdown(wait=False, cancel_futures=True)`로 아직 시작 안 한 항목은 즉시 취소(이미 나간 네트워크 요청은 스레드를 강제로 못 죽여 그대로 완료됨, 데이터 손실 없음). 대시보드에 스피너+상태 라벨+"⏹️ 중지" 버튼을 추가하고 `ui.timer(1.0, ...)`로 `is_sync_running()`을 폴링해 실행 중엔 모든 Sync 버튼을 비활성화. 로컬에서 실제 B&H Sync를 걸고 중지 버튼으로 즉시 취소되는 것까지 실제 클릭으로 검증 완료 후 NAS 배포.
56. **`my.tbd.kr` 500 에러 긴급 수정**: 사용자가 NocoDB를 직접 편집하다가 만든 빈 레코드(SKU=None)를 대시보드가 만나면서 실서비스에 500 에러 발생. NAS 로그에서 `_normalize_store_name()`이 `None.replace()`를 호출해 `AttributeError`가 나는 트레이스백을 바로 확인 - `_unifi_store_url()`이 이름 정규화 매칭을 추가하면서(항목 53) SKU가 없을 가능성을 놓쳤던 것. `_normalize_store_name`에 None 가드 추가 + `build_products_table_html`의 `sku = f.get("SKU") or "-"`로 수정해 NocoDB에 빈/불완전한 레코드가 있어도 대시보드가 죽지 않게 방어. 원인 진단 → 코드 수정 → NAS 배포 → 복구 확인(`/`, `/needs-check`, `/inventory` 전부 200)까지 신속히 완료.
57. **미등록 4개 상품 상세페이지 신규 제작 + 네이버 등록 완료**: "판매가능(In_Stock)인데 상세페이지가 없어서 네이버에 등록 안 된 상품"을 NocoDB/대시보드 기준으로 재조회해 4개 확정(UniFi WiFi BaseStation XG, UniFi G5 Dome Ultra, UniFi AirWire, UniFi G5 Pro) - 이미 Model Number/Category/MSRP_USD/Weight_KG/리테일러 ID와 제품 사진 폴더는 다 갖춰진 상태였음. WebSearch/WebFetch로 Ubiquiti 공식 techspecs.ui.com 스펙을 실제로 조회해 정확한 사양(치수/무게/센서/줌/FOV/야간거리/포트/전력 등)을 확보한 뒤, 유사 카테고리의 기존 Detail 페이지(WiFi 브리지류는 "Device Bridge Switch", G5 카메라류는 "G5 Turret Ultra")를 템플릿으로 `build_pages.py` 컴포넌트 라이브러리를 그대로 사용해 4개 `.dc.html` 신규 작성(공용 섹션 그대로, Hero/Why/Design/Tech Specs만 실제 스펙 기반으로 교체). 각 제품 사진 폴더에서 반사 있는 정면샷은 `crop_hero.py`로 히어로 이미지 크롭, 실제 스펙 콜아웃이 있는 원본 사진을 Design 섹션 다이어그램으로 재사용(별도 그래픽 제작 없이 Ubiquiti 원본 제품샷 활용). `export_sections.py`로 9개 섹션 PNG를 `Product Pages_html/exports/<slug>/`에 렌더링해 스크린샷으로 직접 검증 후 엑셀 템플릿에 4행 추가, `main.py --dry-run`으로 payload 확인 후 실제 등록(4개 전부 성공) → `sync_naver_ids_to_nocodb.py`로 `Naver_Product_No` 반영 → `update_price_stock.py`로 가격/재고 반영(G5 Dome Ultra는 Black 클론 로우가 있어 화이트/블랙 옵션도 자동으로 함께 생성됨) → NocoDB `Product_Page`를 4개 전부 `Detail`로 갱신. 네이버 커머스API로 최종 라이브 상태(SALE/ON, 가격 일치) 확인 완료. 네이버 등록 상품 96개 → 100개.
58. **구매대행 서비스 확장 로드맵 수립 + Phase A(상품등록 공홈 크롤링) 구현/배포**: 사용자가 UniFi 병행수입을 넘어 해외 리테일러 구매대행(주문→현지구매→배송대행지→통관→고객인도) 서비스로 확장할 계획을 밝혀 전체 로드맵(상품등록/상품관리/주문관리/발주배송관리)과 리소스를 먼저 정리(포워더 에코트랜스=API없음/xlsx가능, 알림은 카카오 알림톡 우선, 브랜드는 UniFi+GL.inet부터). 1순위인 Phase A부터 구현: UniFi(store.ui.com)는 Next.js `__NEXT_DATA__`에, GL.inet은 Shopify `{product}.json` 공개 엔드포인트에 이미지/가격/설명이 이미 구조화되어 있음을 조사로 확인해 CSS 스크래핑 없이 JSON 파싱만으로 크롤링하는 `official_scrapers/` 패키지 신축(브랜드별 어댑터, Shopify 어댑터는 향후 다른 Shopify 브랜드에도 그대로 재사용 가능). `search_amazon_candidates.py`의 아마존 검색 로직을 `retailer_search.py`로 이관하고 B&H/Adorama 검색을 추가하는 과정에서 두 사이트 다 검색결과 카드의 `<a>` 태그에 텍스트가 없어 제목 매칭이 항상 실패하는 문제를 발견 - URL 슬러그를 대신 제목 소스로 쓰고 하이픈/언더스코어를 공백으로 정규화해 매칭하도록 수정, 실제 상품(U6 Pro)으로 3개 리테일러 전부 정확한 후보를 찾는 것까지 검증. `dashboard/pages/register.py`에 크롤링 미리보기/이미지 다운로드/리테일러 후보 검색 UI를 추가하고 NocoDB에 `Brand` 필드(사용자가 직접 추가)를 반영, 실제 저장까지 테스트 SKU로 검증 후 삭제. NAS 배포 중 `register.py`의 `import image_uploader`가 연쇄적으로 `naver_config.py`(NAVER_CLIENT_ID/SECRET 필수)를 요구해 **NAS 사이트 전체가 기동 실패**하는 사고 발생 - `image_uploader` import를 클릭 시점으로 지연시키고 원인 진단 후 재배포로 복구. 이어서 사용자가 NAS에 `/volume1/docker/nicegui/dev/smartstore`(Synology Drive로 Google Drive "TBD Seoul" 동기화)를 구성 - docker-compose가 이미 리포 루트 전체(`.:/app`)를 마운트하고 있어 컨테이너 안에서 별도 볼륨 설정 없이 `/app/dev/smartstore`로 바로 보이는 것을 확인. `naver_config.py`에 `TBD_SEOUL_ROOT` 환경변수(.env, NAS만 설정)로 이미지 경로를 오버라이드하는 기능을 추가하고 `NAVER_CLIENT_ID`/`SECRET`도 `required=False`로 완화(실제 쓰는 곳은 로컬 전용 `main.py`뿐이라 NAS엔 필요 없음). NAS `.env`에 새 줄을 추가하는 과정에서 trailing newline 없이 append해 마지막 두 줄이 한 줄로 붙어버리는 사고가 있었으나 즉시 발견/수정. 최종적으로 my.tbd.kr에서 실제 크롤링→이미지 다운로드까지 실행해 `/app/dev/smartstore/Product Images/`에 파일이 정확히 생성되는 것을 확인 후 테스트 데이터 정리. 이제 로컬 맥뿐 아니라 my.tbd.kr을 통해 외부에서도 신규 상품 이미지 크롤링이 가능해짐.
59. **Product Images 폴더명 규칙 통일(SKU → Brand+Model Number) + 기존 189개 일괄 정리**: 사용자가 "이미지 다운로드" 폴더명이 크롤링된 SKU 그대로라 너무 길고 복잡하다고 지적 - `register.py`의 `_image_folder_name()`을 "{Brand} {Model Number}"(예: `UniFi UCG-Ultra`) 형식으로 변경. GL.inet은 폴더명에서 마침표를 빼는데, 처음엔 임의로 "GLiNet"으로 정했다가 사용자가 실제로 만든 폴더("GLiNET GL-BE3600")를 보고 "GLiNET"(대문자 NET)으로 맞춤 - 브랜드 표기는 규칙으로 유추 불가하니 실제 사용례를 확인해야 한다는 교훈. 기존 Google Drive Product Images 폴더 207개 전체를 이 규칙으로 정리: NocoDB 레코드의 SKU(전체/브랜드 접두어 제거/구두점 정규화 3가지 방식)와 폴더명을 대조해 170개를 매칭했고, 그중 15개는 같은 제품에 원본 크롤링 덤프(구버전, 영상/데이터시트 포함, 파일명 안 정리됨)와 이미 정리된 최종본(UniFi 접두사, 모델명 기준 파일명)이 둘 다 있는 걸 발견 - 파일 크기 비교로 완전 동일 콘텐츠임을 확인 후 사용자에게 처리 방식을 물어 "정리된 버전 유지 + 구버전 삭제"로 확정. 실행 후 남은 33개 미매칭 폴더를 파일 크기 집합으로 재대조해 3개(UCG Fiber/Max/Ultra) 추가 발견했으나 삭제 명령이 권한 정책에 막혀 사용자가 직접 삭제. 최종 154개 이름변경 + 19개 중복 삭제로 189개 폴더 전체가 새 규칙으로 정리됨(NocoDB에 아직 없는 나머지는 Model Number를 몰라 보류). `dashboard/pages/register.py` 수정은 my.tbd.kr에도 배포.
60. **Phase C(주문관리) 최초 구축 + NAS 배포 수정**: 구매대행 로드맵 Phase C(주문관리)의 핵심 코드 - `naver_order_api.py`(네이버 Pay-Order/Claims API 클라이언트, `auth.py`의 bcrypt 기반 전자서명으로 bearer token 발급) + `dashboard/pages/orders.py`(`/orders` 페이지 UI) - 를 작업 트리에 미리 구현돼 있던 상태에서 검토 후 커밋. NAS 배포 직후 컨테이너가 `ModuleNotFoundError`로 재시작을 반복하는 사고 발생 - `orders.py`가 module-level에서 `naver_order_api`를 import하고 그게 다시 `auth.py`→`bcrypt`로 이어지는데 `bcrypt`가 Dockerfile pip 목록에 없었던 것. `orders.py`의 import를 `load_orders()`/`load_claims()` 함수 내부로 지연시키는 lazy import 패턴으로 우회하고 Dockerfile에 `bcrypt` 추가 후 재빌드로 복구.
61. **GL.inet 이미지 크롤링 실패 수정 + 사이드바 LINKS 섹션 추가**: GL.inet 상품 등록 시 이미지 크롤링이 조용히 실패하던 원인 발견 - `official_scrapers/shopify.py`가 상품 목록/검색결과 페이지에서 복사한 `/en-us/products/...` 형태 로케일 프리픽스 URL을 그대로 `.json` 엔드포인트에 붙였는데, Shopify `.json`은 프리픽스 없는 `/products/...` 경로에만 존재해 404가 났던 것 - `_product_json_url`이 `/products/` 이후 핸들만 뽑아 재조립하도록 수정. 같은 날 사이드바 하단에 LINKS 섹션(TBD Seoul 스마트스토어/스마트스토어센터/커머스API센터, 외부 링크라 `target="_blank"`로 새 창) 추가.
62. **대시보드 로그인 인증 추가 (CODE_REVIEW.md P0)**: 사용자가 공유한 `CODE_REVIEW.md`(ChatGPT 검토)를 실제 코드와 대조 검증하며 대응 시작 - 가장 시급한 P0로, Cloudflare Tunnel로 `my.tbd.kr`을 외부에 노출 중인데 대시보드에 인증이 전혀 없어 URL만 알면 누구나 `/register`(실제 네이버 API 실행)·`/orders`(고객 주문정보)를 포함한 모든 페이지에 접근 가능했던 문제를 수정. `dashboard/auth.py`의 `AuthMiddleware`가 `app.storage.user` 세션 기준으로 미인증 요청을 `/login`으로 리다이렉트하도록 구현, 단일 관리자 비밀번호 `DASHBOARD_PASSWORD`와 세션 서명 키 `DASHBOARD_STORAGE_SECRET`을 환경변수 필수값으로 추가(로컬/NAS `.env` 각각 설정, 없으면 기동 자체 실패). 로그아웃은 사이드바 LINKS 하단 링크(`/logout`).
63. **Sync: NocoDB 갱신 실패 전파 + Price History 기록 순서 수정 (P1)**: `process_single_record()`가 `table.update()` 예외를 `except Exception: pass`로 삼킨 뒤 무조건 성공 로그를 남겨, 실제로는 Products 테이블이 갱신 안 됐는데도 대시보드/Telegram엔 성공으로 표시될 수 있던 문제. Price_History 기록도 본 테이블 갱신보다 먼저 이뤄져서 갱신이 실패하면 이력만 남고 실제 데이터는 안 바뀌는 두 테이블 불일치가 가능했음. `table.update()`를 먼저 시도해 성공했을 때만 Price_History를 기록하고, 실패 시 재고/확인필요 상태 변화 Telegram 알림도 억제하도록 수정 - `process_single_record` 반환값에 성공 여부를 추가해 `run_tbd_tracker`가 실패 건수를 집계, Sync 요약/Telegram 메시지에 노출. mock으로 실패/성공 양쪽 경로 검증.
64. **Sync: 취소 직후 재시작 시 구세대 워커가 새 결과를 덮어쓰는 레이스 수정 (P1)**: Sync 취소 시 `executor.shutdown(wait=False)`로 이미 시작된 워커를 백그라운드에 남겨둔 채 `run_sync_guarded`가 곧바로 락을 반환했는데, 사용자가 그 직후 Sync를 재시작하면 취소된 이전 세대의 스트래글러 워커와 새 워커가 동시에 NocoDB를 갱신해 오래된 결과가 최신 결과를 덮어쓸 위험이 있었음. 스트래글러 스레드를 강제로 죽일 수는 없어 대신 세대(generation) 번호를 도입 - `run_sync_guarded`가 Sync를 시작할 때마다 세대를 올리고, 각 워커는 NocoDB에 쓰기 직전 자기 세대가 여전히 최신인지 확인해 아니면 조용히 건너뜀. mock으로 레이스 시나리오를 재현해 검증.
65. **주문 "최근 7일" 조회가 실제로는 당일만 조회하던 문제 수정 (P1)**: 네이버 Pay-Order API가 한 번에 최대 24시간 범위만 허용하는데 "최근 7일"을 선택해도 실제로는 오늘 하루치만 조회되고 있었음 - 코드 주석엔 알려진 제약으로 남아있었지만 UI 라벨은 그대로 "최근 7일"이라 사용자가 실제 조회 범위를 오인하기 쉬웠음. `_day_window()` 헬퍼로 하루 단위 구간을 만들고 "최근 7일"은 이 구간 7개를 순차 조회해 `productOrderId` 기준으로 병합(중복 제거)하도록 변경, 일부 구간이 실패해도 나머지는 계속 진행하고 실패한 날짜를 라벨/알림에 표시해 조용한 누락을 방지.
66. **주문 API 시간대 처리 취약점 수정 + 클레임 조회 24시간 제한 검증 추가 (P2)**: `get_product_orders`/`get_product_order_claims`가 전달받은 datetime의 실제 tzinfo와 무관하게 문자열 끝에 무조건 "+09:00"을 붙이고 있어, aware UTC datetime이 전달되면 실제 조회 시간이 9시간 어긋날 수 있는 취약점을 발견 - `naver_order_api._to_kst()`로 naive datetime은 KST 벽시계 값으로, aware datetime은 실제 시각을 유지한 채 KST로 정규화하도록 수정. `get_product_order_claims`엔 `get_product_orders`에 있던 24시간 제한 검증이 빠져있어 함께 추가. `pytz`가 이미 쓰이고 있었지만 `requirements.txt`에 빠져있던 것도 이때 명시.
67. **핵심 순수 함수에 pytest 단위 테스트 추가 (P3)**: 저장소에 자동 테스트가 전혀 없었던 문제 - 외부 HTTP/NocoDB를 mock으로 격리해 이번 코드 리뷰 대응 과정에서 손댄 핵심 로직(NocoDB 갱신 실패 처리, Sync 세대 레이스, 공홈 크롤링 1회 호출, `exclude_clone_rows`, 시간대 정규화, 주문 7일 분할 조회 병합, 화이트/블랙 옵션 가격 로직 등)부터 회귀 테스트 28개 추가(`tests/`, `requirements-dev.txt`). 과정에서 `update_price_stock.py`에 `from __future__ import annotations`이 빠져있어 PEP604 `dict | None` 문법이 Python 3.9에서 import 자체가 실패해 테스트 수집이 막히는 것도 발견/수정.
68. **상세페이지 브리프 초안을 Claude로 자동 생성 (`brief_generator.py`)**: 사용자가 "브리프(태그라인/Why 카드/스펙)를 Claude API로 자동 채울 수 있냐"고 물어봐서, CheapSub(`https://api.cheapsub.im`, Anthropic Messages API 호환 중계 게이트웨이, 사용자가 문서 제공)를 통해 claude-sonnet-5를 호출하는 `brief_generator.py`를 신축. Anthropic SDK를 그대로 쓰되 `base_url`만 CheapSub로 바꾸면 되는 구조(공식 문서에 정확히 이렇게 안내돼 있음). 프롬프트에는 실제 라이브에 쓰인 브리프(`Product Pages_html/briefs/*.json`)를 few-shot 예시로 넣어 톤/구조를 맞추고, `tool_choice`로 `submit_brief` 도구 호출을 강제해 JSON 파싱 실패 위험을 없앰(스펙은 참고자료에 없으면 지어내지 말라고 프롬프트에 명시). `/detail-page-builder`에 "🤖 브리프 초안 생성" 섹션을 추가해, 공홈 URL을 넣으면 기존 `official_scrapers.fetch_product()`로 설명/스펙을 가져와 참고자료 칸을 채우고(직접 붙여넣기도 가능), 생성 버튼을 누르면 태그라인/Why 3카드/Design/Tech Specs 전체 폼 필드를 자동으로 채운다. 실제 U6 Pro URL로 크롤링→생성까지 대시보드에서 직접 클릭해 검증(스펙 15개 항목이 실제 크롤링된 값과 정확히 일치하는 것까지 확인). `anthropic` 패키지는 로컬 전용 기능이라(이 페이지 자체가 이미 Playwright처럼 NAS 미지원) `requirements.txt`에만 추가하고 NAS `Dockerfile`엔 넣지 않음 - import도 다른 로컬 전용 기능과 동일하게 버튼 핸들러 안에서만 지연 로드. `CHEAPSUB_API_KEY`는 `.env`에 required=False로 추가(없으면 이 기능만 비활성화, 나머지 대시보드는 정상).
69. **CheapSub 키 교체 + 브리프 자동생성 my.tbd.kr 배포**: 사용자가 토큰 사용량 추적용 전용 키를 줘서 로컬 `.env`의 `CHEAPSUB_API_KEY`를 교체(호출 테스트로 즉시 확인). 이어서 "my.tbd.kr에도 올라갔냐"는 질문에 확인해보니 처음 설계대로 로컬 전용으로 남겨둔 상태였음 - 사용자에게 물어보니 등록 파이프라인 관련 아직 미검증인 별도 작업(Phase B)이 같은 `detail_page_builder.py` 파일에 섞여 있는 걸 감수하고 "전체 파일 그대로 배포"를 선택. `anthropic`이 새 pip 패키지라 NAS Dockerfile에 추가 후 `docker-compose up -d --build`로 재빌드(단순 restart로는 안 됨), NAS `.env`에 `CHEAPSUB_API_KEY` 추가. 배포 직후 실제 브리프 생성 버튼이 조용히 실패 - 원인 추적해보니 `config.py`(CHEAPSUB_API_KEY 정의부)를 NAS에 올리는 걸 빠뜨려서 `AttributeError`가 나고 있었음(대시보드 자체 로그엔 안 남고 컨테이너 안에서 직접 재현해서 발견), `config.py` 재배포로 해결. U6 Mesh URL로 my.tbd.kr에서 크롤링→브리프 생성 전체 흐름을 브라우저로 직접 클릭해 최종 검증.
70. **`/orders` 페이지에 신규 주문이 안 뜨던 버그 수정**: 사용자가 스마트스토어에 실제 주문이 들어왔는데 대시보드엔 안 보인다고 제보. 네이버 API를 직접 호출해 실제 주문 데이터가 API 쪽엔 존재함을 확인하고 원인 추적한 결과, 독립된 버그 두 개가 겹쳐 있었음: (1) `naver_order_api.get_product_orders()`가 실제 응답 구조({"data": {"contents": [{"content": {"order": {...}, "productOrder": {...}}}]}} 식으로 중첩·래핑됨)를 잘못 가정해 매번 0건으로 파싱되고 있었고, (2) `dashboard/pages/orders.py`가 설치된 NiceGUI(3.15.0)엔 없는 `ui.run.io_bound`를 호출해(올바른 API는 `nicegui.run.io_bound`) 매번 `AttributeError`가 났는데 감싸고 있던 bare `except Exception`이 로그 하나 없이 삼켜서 원인 파악을 어렵게 만듦. 둘 다 Phase C 개발 당시 실제 주문으로 검증 못 해본 채 커밋됐던 것으로 보임 - 아마 그동안 들어온 주문이 전부 대시보드에 안 보이고 있었을 가능성이 있음. 응답 언래핑+평탄화 함수 추가, `run.io_bound`로 수정, 실패 시 최소 print 로그 추가 후 실제 주문(2026080346978511)으로 `/orders`에 정상 표시되는 것까지 확인. 클레임 조회 API(`/product-order-claims`)도 404가 나는 걸 발견했으나 정확한 엔드포인트를 못 찾아 별도 이슈로 남겨둠(공식 문서 사이트 접근 불가).
71. **GL.iNet 브랜드 지원 + 상세페이지 제작 도구 + Sync/스마트스토어 페이지 연결**: 작업 트리에 미리 구현돼 있었지만 커밋 안 된 상태였던 기능들을 검토·검증 후 커밋 - `dashboard/pages/brand.py`(`/brand/unifi`, `/brand/glinet` 브랜드별 상품 리스트, 검색/정렬/카테고리 필터), `product_pages/scripts/build_pages.py`(`HEAD`/`TRUST_TO_FOOTER`/`hero()`/`why_section()`을 `UNIFI_BRAND`/`GLINET_BRAND` 딕셔너리로 파라미터화, 기존 UniFi 출력과 byte-identical 확인), `product_pages/scripts/build_detail_page.py` + `dashboard/pages/detail_page_builder.py`(브랜드 무관 "콘텐츠 브리프" 기반 상세페이지 제작 도구, `/detail-page-builder`), GL.inet 최초 상세페이지(Slate 7) 제작 스크립트, `dashboard/pages/sync.py`(기존에 흩어져 있던 Sync 버튼들을 `/sync` 페이지로 정리), `dashboard/pages/smartstore.py` + `sync_naver_status.py` + `sync_naver_price.py`(네이버 등록 전체 상품을 판매상태/가격/재고 기준으로 관리하는 `/smartstore` 페이지 - `sync_naver_price.py`는 원래 하드코딩된 환율(1446.6)과 임의 재고값(999)으로 직접 PATCH하고 dry-run/limit 안전장치가 전혀 없어 라이브 데이터를 잘못 덮어쓸 위험이 있었는데, `update_price_stock.py`의 검증된 GET-전체조회→PUT-전체 로직을 재사용하고 dry-run(기본값)/limit/확인 다이얼로그를 추가해 다른 데이터 수정 스크립트와 동일한 안전 수준으로 재작성). 로컬에서 각 페이지 렌더링 확인 + 가격/재고 업데이트 버튼을 dry-run으로 실제 클릭해 100개 상품 미리보기 검증 완료.
72. **구매대행 로드맵 Phase B 커밋 + 위험한 미완성 코드 발견/수정**: 사용자가 "개발 계획 계속 진행"을 요청해 로드맵 확인 후 Phase B(상품관리 확장)를 선택. 작업 트리를 살펴보니 이미 상당 부분 구현돼 있었음(`sync_engine.py`의 Brand 분기 크롤링, `register.py`의 등록 파이프라인 버튼, `product_keywords.py` 신규 카테고리 등 - 전부 여러 파일에 걸쳐 커밋 안 된 채 쌓여있었음). 전체를 검토하는 과정에서 두 가지를 발견: (1) `sync_engine._run_naver_status_sync()`가 존재하지 않는 `_send_telegram_message()`를 호출하는 버그(스케줄러 미등록이라 아직 크래시는 없었음) - `send_telegram_msg`로 수정. (2) **`/smartstore` 페이지의 "가격/재고 업데이트" 버튼이 위험한 미완성 상태** - 호출하는 `sync_naver_price.py`가 하드코딩된 환율(1446.6)과 임의 재고값(999/0)으로 네이버 API에 필드 2개만 직접 PATCH하고 있었고, 이 프로젝트의 다른 모든 데이터 수정 스크립트가 지키는 dry-run/limit 안전장치가 전혀 없어 클릭 즉시 라이브 상품을 잘못된 값으로 덮어쓸 위험이 있었음 - 사용자에게 알리고 확인받은 뒤, `update_price_stock.py`의 검증된 GET-전체조회→PUT-전체 로직(화이트/블랙 옵션 처리 포함)을 재사용하도록 전면 재작성하고 dry-run(기본값)/limit/확인 다이얼로그를 추가함. 커밋은 성격별로 3개로 분리(신규 페이지 연결/Phase B 핵심 로직/디자인 개편 - 다크모드 제거 포함, 원래 별개 작업이었음). Dry-run으로 실제 100개 상품 미리보기까지 검증 완료. 구매대행 로드맵 Phase A/B/C 전부 완료, 남은 건 Phase D(발주배송관리)뿐.
73. **대시보드 홈 화면 개편 + 다크모드 제거 + requirements.txt 정리**: 구매대행 로드맵과 무관한 별도 디자인 정리 작업(기존 "대시보드 디자인 디테일 폴리싱" 항목) - 작업 트리에 미리 구현돼 있던 것을 검토 후 커밋. `dashboard/pages/home.py`/`components.py`의 상단 통계를 환율 라인차트/Scrape.do 도넛차트/상태 도넛차트/브랜드별 상세페이지 커버리지 바로 교체. `dashboard/theme.py`에서 다크모드 지원을 제거해 단일 라이트 테마로 통일(로고도 `tbd_logo.svg` 단일 에셋으로 교체) - `category.py`/`inventory.py`/`needs_check.py`의 `_theme_name()` 참조 제거, 최신 NiceGUI가 `ui.html()`을 기본 sanitize하면서 깨지던 커스텀 테이블 마크업에 `sanitize=False` 추가. `requirements.txt`도 한참 전 Streamlit→NiceGUI 전환(항목 4) 이후 갱신이 안 돼있던 걸 실제 의존성에 맞게 정리, streamlit 제거.
74. **NAS에 네이버 시크릿 추가 - "실사용은 항상 my.tbd.kr" 운영 원칙 확립**: 커밋+배포 요청 뒤 사용자가 스크린샷으로 `/orders` 페이지의 실패("총 0건 조회 실패", "클레임 조회 실패: 'NoneType' object has no attribute 'encode'")를 제보. 로컬에서 재현 안 돼서(주문 1건 정상 조회) my.tbd.kr에서 난 문제로 특정 - NAS `.env`에 네이버 시크릿이 없어 `get_bearer_token()`이 `CLIENT_SECRET=None`으로 bcrypt 해싱을 시도하다 난 에러였음. 처음엔 "이 기능은 로컬 전용"이라는 안내 메시지로 우아하게 처리하려 했으나, 사용자가 "로컬에서 실행할 일이 없고 모든 걸 my.tbd.kr에서 해결하고 싶다"고 명확히 밝혀 방향을 바꿔 NAS `.env`에 실제 `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`을 추가함(이제 등록 파이프라인 버튼도 my.tbd.kr에서 실제로 작동함 - dry-run/확인창은 이미 있음). 시크릿 값에 bcrypt 해시 특유의 리터럴 `$`가 들어있어 `docker-compose`가 자체 변수치환 경고를 냈지만, 이 프로젝트는 `environment:`가 아니라 python-dotenv로 직접 읽으므로 실제 값은 안 깨짐을 컨테이너 안에서 직접 확인. `naver_order_api._get_headers()`에 시크릿 누락 시 명확한 에러 메시지를 내는 가드도 추가(방어적으로, 실제로는 이제 NAS에도 시크릿이 있어 트리거 안 됨). CLAUDE.md에 "로컬 전용을 기본값으로 두지 말 것"이라는 운영 원칙을 명시 - PNG Export(Playwright)만 유일하게 남은 로컬 전용 기능.
75. **등록 파이프라인 섹션을 `/register`에서 `/smartstore`로 이동**: 사용자가 "신규등록 페이지 아래에 있는 등록 파이프라인 부분은 스마트스토어 페이지 상단으로 옮겨줘"라고 요청. `register.py`의 "🛠️ 등록 파이프라인" 블록(모듈 레벨 락/상태 딕셔너리, dry-run/limit 컨트롤, 4개 버튼, subprocess 실행기, 확인 다이얼로그, 상태 폴러 전체)을 `smartstore.py` 상단으로 그대로 옮김. `register.py`는 신규 상품 크롤링/등록 흐름만 남아 더 짧아졌고, 옮기며 안 쓰게 된 `subprocess`/`sys`/`threading` import를 정리함.
76. **`/smartstore` 페이지 버튼 중복 제거 + 역할 설명 추가**: 사용자가 "페이지의 네이버 상태 동기화 그리고 가격/재고 업데이트 버튼과 기능적으로 겹쳐. 하나로 합쳐줘. 그리고 각 버튼이 어떤 역할을 하는지 알기 쉽게 써줘"라고 요청. 등록 파이프라인 섹션의 "가격/재고 갱신" 버튼(`update_price_stock.py`를 subprocess로 실행, `TARGET_PRODUCTS`로 범위가 좁음)이 같은 페이지 하단의 기존 "가격/재고 업데이트" 버튼(`sync_naver_price_stock()` 직접 호출, `Naver_Product_No`가 있는 전체 상품 대상이라 범위가 더 넓고 이미 이번 세션에서 dry-run/limit 안전장치까지 갖춘 완성형)과 완전히 겹치는 걸 확인 - 후자만 남기고 전자(버튼/핸들러/`_pipeline_buttons` 등록)를 제거함. 남은 모든 버튼(상품 등록/전체 파이프라인/배송 설정 수정/네이버 상태 동기화/가격·재고 업데이트) 아래에 한 줄 설명을 달아 비개발자도 각 버튼의 역할과 데이터 흐름 방향(네이버→NocoDB 읽기전용 vs NocoDB→네이버 라이브 반영)을 바로 알 수 있게 함. 로컬 브라우저 + my.tbd.kr 양쪽에서 렌더링 확인 후 배포(코드만 바뀐 변경이라 `docker-compose restart`로 충분).
77. **CLAUDE.md/HISTORY.md 정리 (번호 누락·순서 오류 수정 + 미기록 작업 11건 보강)**: HISTORY.md 항목 번호가 61 → 63 → 64 → 62(순서 역행) → 65 → 66으로 꼬여있던 걸 발견 - 실제 git 커밋 시각 기준으로 전부 재정렬. 대조 과정에서 CLAUDE.md의 "현재 상태" 섹션엔 서술돼 있지만 HISTORY.md엔 한 번도 기록된 적 없는 작업이 다수 발견됨(Phase C 주문관리 최초 구축, 대시보드 로그인 인증, CODE_REVIEW.md P1~P3 대응 5건, GL.iNet 브랜드 지원+상세페이지 제작 도구, 대시보드 홈 개편+다크모드 제거 등 - 위 항목 60~67/71/73) - git log와 각 커밋 메시지를 대조해 시간순으로 끼워넣어 보강. CLAUDE.md의 "지난 세션들의 상세 작업 이력은 HISTORY.md 참고"라는 원래 설계를 실제로 지키도록, CLAUDE.md 현재 상태 섹션도 함께 축약(장문 서술은 HISTORY.md 항목 번호로 대체, 현재 유효한 사실/설정값만 유지).

78. **대시보드 UI/디자인 시스템 통일**: 사용자가 Obsidian에 정리해둔 "TBD Pipeline.md" 관리 플로우 문서를 기준으로 현재 대시보드를 감사(Explore 에이전트 2개 병렬 조사) - 통계 카드/버튼/로그/섹션 헤더가 페이지마다 최소 3~4가지 스타일로 제각각이었고, 심지어 읽기전용(안전) 액션보다 라이브 API 쓰기(위험) 액션이 덜 눈에 띄는 스타일이던 역전 문제도 발견. `dashboard/theme.py`에 로그 크기 모디파이어(`.tbd-log--fill/md/sm`)와 카드 타이틀 클래스 추가, `dashboard/components.py`에 `primary_button`/`safe_button`/`live_write_button`/`utility_button`/`section_header`/`confirm_dialog` 헬퍼 추가해 전 페이지(sync/login/smartstore/register/detail_page_builder/home)에 적용. **시맨틱 버튼 색을 처음엔 새 CSS 클래스(`.tbd-btn-*`)로 시도했으나 실패** - Quasar의 기본 `color=primary` 배경이 자체 `!important` 규칙을 쓰고 있어 새 `<style>` 블록 클래스로는(specificity를 `.q-btn.tbd-btn-*`로 올려도) 못 이김을 실제로 겪음. 이 프로젝트에서 검증된 유일한 override 수단인 Tailwind `!bg-[...]` 임의값 클래스(기존 sync.py 검정 버튼이 쓰던 방식)로 전환해서 해결. smartstore.py의 위험도-스타일 역전(라이브 쓰기 버튼이 outline, 읽기전용이 굵은 초록)도 이때 수정(`live_write_button`/`safe_button`으로 교체).
79. **상품관리 기능 격차 보완**: 같은 문서 대비 감사에서 나온 자잘한 격차들을 한 번에 처리. (1) `detail_page_builder.py`에 `ui.upload`로 직접 이미지 업로드 추가(공홈 크롤링 없이도 파일을 바로 Product Images 폴더에 추가, 기존 리딩넘버 뒤로 이어붙임). (2) 브랜드 로고/구매·배송 안내 문구를 코드 수정 없이 바꾸는 `/settings` 페이지 신설 - 새 `common_settings.py`(JSON 저장, 기본값은 기존 하드코딩 문구 그대로 복제)를 `build_pages.py`가 병합해서 씀(트러스트 템플릿의 "2주"/"7일에서 14일" 등도 토큰화). (3) `sync.py`에 상단 통계 카드(상품수/판매가능/품절/확인필요) 추가 - `category.py`/`brand.py`가 쓰는 것과 동일한 `stat_card()` 패턴. (4) `smartstore.py`에 "등록대기"(상세페이지는 있지만 `Naver_Product_No` 없는 상품) 카드+리스트 추가 - `detail_page_builder.py`의 `_list_completed_pages()`를 `build_detail_page.py`의 공개 함수로 승격해 공유. (5) 문서에 명시된 대로 "배송 설정 수정" 버튼(및 관련 핸들러) 삭제, `fix_delivery_settings.py`는 `archive/scripts/`로 이동. (6) `orders.py`의 주문 상품명을 리테일러 사이트 링크로 - `sync_engine`의 기존 URL 빌더(`_unifi_store_url`/`_bh_url`/`_adorama_url`/`_amazon_url`) 재사용, 이 김에 네이티브 `ui.table`을 사이트 공통 `uic-table` HTML 패턴으로 전환.
80. **주문관리 발주·배송 신규 구축 (카카오 메시지 제외)**: 지금까지 `/orders`는 네이버 주문 조회만 가능했고 발주/배송 상태를 저장할 DB 자체가 없었음. 사용자가 제공한 에코트랜스 배송대행지 신청서 샘플(`iecot_sample.xls`, 37컬럼 "업로드양식" 시트)과 품목분류코드표(`iecot_품목분류코드.csv`)를 분석해 실제 스키마를 확보한 뒤 구축. 새 NocoDB 테이블 `Order_Fulfillment`(`create_order_fulfillment_table.py`, `create_price_history_table.py`와 동일한 멱등 생성 패턴, `NOCODB_ORDER_TABLE_ID` 로컬 `.env`에 추가) - 주문(네이버 자동수집)→발주(현지 주문번호/단가 수동입력)→현지배송(현지 송장번호 - 입력되면 "현지배송 시작"으로 간주, 문서 규칙 그대로)→배송대행지 신청(에코트랜스 xlsx)→국제배송 송장→네이버 발송처리까지 한 로우로 추적. `order_fulfillment.py`(주문판 sync_engine - `day_window`/`merge_orders_by_id`를 `orders.py`에서 승격해 공유, `match_sku_for_order`로 네이버 주문명↔NocoDB SKU 매칭, `derive_fulfillment_status`는 저장 필드가 아니라 매번 계산). `iecot_config.py`에 사람이 확정한 값들(`IECOT_ITEM_CODE="91"`, `IECOT_BRANCH_CODE="DE"`, `IECOT_SALES_AGENT_NAME="TBD Seoul"`). 새 페이지 `/purchase`(발주 대기/현지배송 등록 폼)와 `/shipping`(체크박스 선택 후 `iecot_export.py`로 37컬럼 xlsx 생성 - 원본은 레거시 OLE2 `.xls`라 openpyxl로 못 열지만 컬럼 구조만 참고해 새 `.xlsx`를 처음부터 생성, 국제송장 등록, 그동안 어디서도 안 쓰이던 `naver_order_api.dispatch_product_order()`를 처음 연결하는 "스마트스토어 등록" 버튼 - 라이브 쓰기라 미리보기 토글+확인 다이얼로그 필수). 사이드바 SALES를 주문→발주→배송 서브메뉴로 확장(PRODUCTS의 UniFi/GL.iNet 서브메뉴와 동일 패턴). 실제 주문 1건으로 발주→현지배송→신청서생성→국제송장→발송미리보기 전 단계 End-to-end 검증 완료(테스트 데이터는 삭제). **NAS 배포 시 Dockerfile에 `openpyxl` 추가 필요**(xlsx export가 NAS에서도 동작하려면 필수) - 이미 추가함. 카카오 알림톡은 API 키/템플릿 승인 대기 중이라 이번 범위에서 완전히 제외.

81. **주문 카드 개편(7단계 통계/자유 날짜범위/페이지네이션) + ACE Express 실시간 배송조회 연동 + 후속 보정**: 항목 80 배포 직후 이어진 후속 요청들 - (1) 현지배송이 배송대행지 신청/국제송장보다 늦게 나오는 경우가 많다는 사용자 설명에 맞춰 `/shipping`의 "현지배송 등록" 섹션을 "국제배송 송장 등록" 아래로 재배치하고 게이팅 조건도 `local_tracking_number`가 아니라 `local_order_number` 기준으로 변경. (2) 에코트랜스 신청서의 우편번호/주소/연락처를 네이버 주문 상세에서 자동으로 채우도록 `naver_order_api.get_recipient_info()` 추가 - 이 과정에서 `get_product_order_detail()`이 처음부터 잘못된 엔드포인트(`GET .../product-orders/{id}`, 항상 404)를 쓰고 있었던 걸 발견, 올바른 엔드포인트(`POST .../product-orders/query`)로 수정. 개인통관고유부호는 네이버 API 응답 전체를 덤프해봐도 필드 자체가 없음을 확인 - "해외" 상품 항목이 스마트스토어센터에서 아직 비활성화라는 사용자 설명으로 원인 확정. (3) 사이드바 SALES의 발주/배송을 주문의 하위 서브메뉴가 아니라 동급 항목으로 평탄화. **이후 사용자가 주문/발주/배송 3개 페이지를 한 번에 대폭 요청**: `/orders`를 테이블→카드 형식으로 재설계(주문일시/주문번호/주문자명/전화번호/주소/개인통관고유부호 노출, 7단계 배송단계 그래픽 트래커, 통합검색, 클레임 목록 섹션은 완전히 제거), `/purchase`에 리테일러 가격비교 표(공홈/Amazon/Adorama/B&H, 클릭 시 새 탭) 추가. 검증 중 실제 주문의 `naver_dispatch_result`에 실제 400 에러 흔적을 발견해 보고했더니, 사용자가 "이미 스마트스토어에서 직접 국제배송 송장을 등록해서 그런 것 같다"고 확인 - 해당 필드를 수동완료로 보정. 이어서 **택배사 코드 요청** - 스마트스토어센터가 제공하는 공식 택배사 코드표 xls(222개)를 `naver_delivery_companies.json`으로 추출해 `/shipping`에 선택 드롭다운 추가(기본값 ACE) - 이 과정에서 `naver_order_api.py`에 있던 기존 `DELIVERY_COMPANIES` 딕셔너리(10개, 어디서도 안 쓰이던 죽은 코드)가 공식 코드표와 실제로 값이 어긋나 있었음(로젠택배 실제 코드는 KGB인데 LOGEN으로 잘못 기재)을 발견해 제거. **가장 큰 후속 요청**: 사용자가 제공한 ACE Express(acedp.co.kr) 크롤링 스크립트 초안을 기반으로 실제 동작하는 송장조회 엔드포인트(`GET /welcome/Track/hno/{송장번호}` - 사이트의 `Trackit()` JS 함수 소스를 직접 덤프해서 찾음)를 확인하고 `ace_express_tracking.py`로 완성, 사용자가 알려준 실제 송장번호 2건(우체국 국내배송/CJ택배 국내배송)으로 검증. `/orders`를 상단 7단계 카운트 카드 + 자유 날짜범위(프리셋 대신 시작일/종료일, 기본 최근 30일) + 15건 초과 시 페이지네이션으로 재설계하고, 국제배송/통관/국내배송 단계를 ACE 실측 이벤트(항공기 출발/도착/반출) 기준으로 판정하도록 변경(한 번 확인된 단계는 `Order_Fulfillment`에 저장해 재조회 생략). 검증 과정에서 **네이버 API 429 Too Many Requests가 30일 연속 조회 중 실제로 재현**됨을 발견(31개 호출 중 최대 20개까지 실패한 적도 있음) - `get_product_orders()`에 지수 백오프 재시도(최대 5회) + `/orders` 호출 루프에 0.8초 텀을 추가해 완화. 마지막으로 택배사 선택 위치를 "스마트스토어 등록" 단계에서 "국제배송 송장 등록" 단계로 옮기고(`intl_delivery_company`에 저장), 발송 처리 시엔 그 값을 자동 재사용하도록 변경. 31개 테스트 전부 통과, my.tbd.kr 배포 및 3개 페이지 실제 데이터로 재검증 완료.

82. **`/orders` 로딩 성능 개선 + 에코트랜스 상품명 영문화 + 상세페이지 로고 교체**: 사용자가 3건을 한 번에 요청. (1) **주문 페이지 로딩 속도**: 원인은 주문마다 수령인정보(네이버 주문상세 API) + ACE Express 배송조회를 `for` 루프 안에서 하나씩 순서대로 `await`하고 있던 것 - 네트워크 호출이 병렬이 아니라 직렬로 쌓여 주문 수만큼 로딩시간이 선형으로 늘어났음. 사용자가 "배송 크롤링 데이터를 DB에 저장해서 띄워주는 방식"을 제안했는데, 확인해보니 `Order_Fulfillment`엔 이미 `recipient_name_kr`/`recipient_phone`/`recipient_postal_code`/`recipient_address`/`personal_customs_code` 컬럼이 있었음(발주 시점에 `/purchase`가 채워두는 용도로 이미 존재 - 새 스키마 불필요). `orders.py`에 `_fetch_order_extras()`를 신설해 (a) 이 필드들이 이미 채워져 있으면 네이버 API를 다시 안 부르고 캐시된 값을 쓰고, (b) 캐시 미스인 주문들만 `asyncio.gather()`로 동시에 조회하도록 변경(순차 합산 → 최댓값에 수렴). 새로 조회한 수령인정보는 로우가 있으면 `update_fields`로, 없으면(아직 발주 전) `find_or_create`로 최소 필드만 채워 저장. 이 과정에서 `find_or_create`가 기존 로우를 찾으면 `defaults`를 완전히 무시하던 것을 발견 - `/orders`가 먼저 만든 부분 로우(수령인정보만 있음)를 나중에 `/purchase`가 발주하면서 `find_or_create`를 호출해도 `sku`/`naver_product_name`/`quantity` 등이 채워지지 않는 버그가 생길 뻔했음 → 이미 있는 로우도 defaults 중 "값이 있는데 로우엔 비어있는" 필드만 backfill하도록 `find_or_create`를 확장(이미 값이 있는 필드는 절대 덮어쓰지 않음). 로컬에서 실제 로그인 후 `/orders`를 열어 캐시 적중 경로(수령인정보가 이미 있는 발주완료 주문)로 정상 렌더링됨을 검증. (2) **에코트랜스 xlsx 상품명 영문화**: 통관 절차상 "상품명(영문)" 컬럼에 한글이 들어가면 안 되는데, 기존엔 네이버 등록명(`naver_product_name`, `product_keywords.py` 규칙상 "영문명 / 한글명 키워드..." 형식이라 한글이 섞여있음)을 그대로 쓰고 있었음 - `iecot_export.py`에 `_english_product_name()`을 추가해 NocoDB `SKU`(항상 영문, 발주 시점에 이미 저장돼 있음)를 우선 쓰고, 없으면 `naver_product_name`의 " / " 앞부분(영문 구간)만 잘라 쓰도록 수정. (3) **상세페이지 헤더/푸터 로고 교체**: 사용자가 제공한 Google Drive `TBD Seoul/Assets/`의 신규 로고 3종(`tbd_logo_gray.svg`=푸터, `unifi_tbd.svg`/`glinet_tbd.svg`=브랜드별 헤더 콜라보 로고)을 `Product Pages_html/assets/common/`에 반영 - 푸터는 파일명을 그대로 유지한 채(`common_logo-footer.svg`) 내용만 교체해 공용 asset이라 이미 생성된 103개 `.dc.html`에도 자동 소급 적용됨(이미지는 상대경로 참조라 텍스트 문구와 달리 소급 적용 안 되는 제약이 없음). 헤더는 기존 "브랜드 로고 + 구분선 + 작은 TBD 로고" 2단 구성을 콜라보 로고 1개로 구조 자체가 바뀌어서(`build_pages.py`의 `head()` 템플릿과 `UNIFI_BRAND`/`GLINET_BRAND` 수정) 기존 103개 파일은 1회성 배치 스크립트로 별도 일괄 치환. 이 과정에서 `common_settings.py`의 `DEFAULTS`가 `build_pages.py`와 별도로 구버전 로고 경로를 중복 정의하고 있던 걸 발견 - `/settings`에 저장된 커스터마이징이 없으면(로컬/NAS 둘 다 `common_settings.json` 미존재 확인) `common_settings.load()`가 이 구버전 `DEFAULTS`를 그대로 반환해 방금 고친 `build_pages.py` 값을 다시 덮어쓸 뻔했음 - 두 파일을 동기화해서 해결. 로컬 정적 서버로 렌더링 검증(헤더/푸터 이미지 모두 정상 로드, 새 비율 확인) 후 3건 전부 my.tbd.kr에 배포(`docker-compose restart`, Dockerfile 변경 없어 재빌드 불필요).

83. **`/orders` 로딩 성능 개선 2차**: 항목 82의 캐싱/병렬화 배포 후에도 사용자가 "여전히 느리다"고 재보고. 로컬에서 실측 타이밍을 다시 재보니 82에서 고친 "주문별 부가정보(수령인정보/ACE) 조회" 단계는 이미 충분히 빨랐고(테스트 데이터가 주문 1건뿐이라 체감이 거의 없었음), 진짜 병목은 그 앞 단계인 "주문 목록 자체 조회"였음 - 네이버 API가 1회 호출당 최대 24시간만 허용해 `date_range_windows()`로 하루씩 쪼갠 뒤, 항목 81에서 429 방지용으로 넣어둔 "호출 사이 0.8초 텀 + 순차 호출"이 기본 30일 범위 기준 30번 왕복 + 29×0.8초 ≈ 20초 이상을 그냥 대기시간으로 날리고 있었음(90일 범위면 91번 왕복 + 72초). `orders.py`의 날짜창 조회 루프를 `asyncio.Semaphore(3)`로 최대 3개 동시 호출하도록 바꾸고 고정 텀을 제거 - 완전 무제한 동시요청은 피해서(예전에 텀 없이 31개를 한꺼번에 쏴서 429가 대량 발생한 전례가 있음, 항목 81) 개별 호출에 이미 내장된 지수 백오프 재시도가 남은 429를 흡수하게 함. 로컬에서 실제 로그인 후 타이밍 측정 - 기본 30일 범위 로딩이 30~40초 → 8~10초로, 90일 범위(91개 날짜창)도 에러 없이 완료됨을 확인(두 경우 모두 조회 실패 0건). my.tbd.kr 배포 완료.
