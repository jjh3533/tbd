version: '3.8'

services:
  nicegui:
    build: .
    container_name: nicegui_app
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - nicegui_storage:/app/.nicegui

volumes:
  nicegui_storage:
